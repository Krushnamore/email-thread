fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.9.2
reportlab==4.2.2
python-multipart==0.0.9
requests==2.32.3
python-whois==0.9.4

# --- ML is the primary detection engine now (see backend/ml/) ---
# Runtime inference deps:  pip install -r requirements-ml.txt
# Training-only deps:      pip install -r requirements-train.txt
# (kept separate on purpose -- see the comment at the top of
# requirements-ml.txt for exactly why)
